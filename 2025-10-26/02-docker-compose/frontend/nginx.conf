server {
    root /usr/share/nginx/html;        # set the root directory for static files (the React build folder)

    location / {                       # handle all requests from the root path
        try_files $uri /index.html;    # if the requested file doesn't exist, serve index.html (SPA fallback)
    }
}
